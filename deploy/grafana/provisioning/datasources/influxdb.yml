apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    isDefault: true
    jsonData:
      version: Flux
      organization: twin
      defaultBucket: telemetry
    secureJsonData:
      token: dev-token-change-me
