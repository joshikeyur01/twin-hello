# Mosquitto config for twin-hello.
# Localhost-only, no auth. twin-services will replace this with mTLS.

listener 1883 0.0.0.0
allow_anonymous true
persistence false
log_dest stdout
log_type warning
log_type error
